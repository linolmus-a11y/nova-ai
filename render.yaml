services:
  - type: web
    name: nova-ai
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: NOVA_MODEL
        value: gpt-5.6-luna
